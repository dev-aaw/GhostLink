# Rust build artifacts
target/
**/*.rs.bk
# Cargo.lock IS committed: GhostLink ships as an application, so builds must be
# reproducible (pinned dependency graph) for release binaries.

# Operating System Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo

# GhostLink local runtime & dynamic data (root only)
/bin/
/lists/
.ghostlink/
/temp/
smart_routing.json
*.log

# Sensitive WireGuard / Tunnel Credentials (NEVER commit secrets!)
*.conf
*.key
wg*.conf
privatekey
publickey
presharedkey
GhostLink_v*_Windows/
GhostLink_v*_Windows*.zip
